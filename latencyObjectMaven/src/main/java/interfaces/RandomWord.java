package interfaces;

/**
 * 
 * @author wfristdr
 * 
 * From word, to line, to text file (1000 lines),
 * and finally to the zip of 5 copied text files.
 *
 */

public interface RandomWord {
	
	/**
	 * 
	 * @return a single random word
	 */
	String createWord();
}
